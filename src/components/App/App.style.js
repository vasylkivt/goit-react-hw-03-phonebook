import Notification from 'components/Notification/Notification';
import styled from 'styled-components';

export const NotificationForm = styled(Notification)``;
export const NotificationContactList = styled(Notification)`
  margin: 0;
`;
